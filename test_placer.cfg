@ 1000000	Number of particles (per processor).
@ 500		Number of cells in X direction.
@ 500		Number of cells in Y direction.
@ 500		Number of cells in Z direction.
@ 1		Boundary condition (xMin).
@ 0		Boundary condition (yMin).
@ 0		Boundary condition (zMin).
@ 1		Boundary condition (xMax).
@ 0		Boundary condition (yMax).
@ 0		Boundary condition (zMax).

  Type of boundary condition is:
    0 - periodic;
    1 - mirror;
    2 - Mur's first order absorbing boundary condition.
    3 - Liao absorbing boundary condition.
