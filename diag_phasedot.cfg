@ 0		Start record.
@ -1		Finish record (if negative then use the last one).
@ 1		Step size (>0).
@ 1		Do generate plt file directly (1) or write ASCII file (0).
@ 0		System of units (1 - micron/fs, 0 - lambda/t0).
@ 1e20		Desired q/M ratio for component to show (use > 1e10 to show all).
