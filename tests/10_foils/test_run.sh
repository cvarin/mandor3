#!/bin/sh

echo "It is 'total field/scattered field' interface test. Main idea is "
echo "to record field in the region bounded by interface and than to"
echo "play it. Source of the radiation is a gaussian spot, so it gives"
echo "gaussian pulse for sizes bigger than wavelenght (it is not a "
echo "correct solution, just temporary hack)."
echo;
echo "To run recorder session use test_rec.sh"
echo "To run player   session use test_play.sh"
echo;
