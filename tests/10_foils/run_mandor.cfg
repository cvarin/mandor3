@ 1000		Total number of steps
@ 2000		Period between checkpoints
@ 100		Period between TecPlot snapshots (negative means no additional writing of TecPlot snapshots to disk)
@ 1		Continue run from last checkpoint (1 - yes, 0 - no)
@ 20		Check Stop Flag every $< steps
@ 250		Dump period (time steps) for spectral density visualization.
