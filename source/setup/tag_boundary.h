/** \file tag_boundary.h
  * \brief Sets boundary conditions by call to parameter_setBounds(). All checks are actually done there.
  */

#ifndef tag_boundary_header
#define tag_boundary_header						///< Usual guard construction.

#include <stdio.h>

void tag_boundary (FILE *fp);

#endif
