@ 0		Start record.
@ -1		Finish record (if negative then use the last one).
@ 1		Step size (>0).
@ 1		System of units (1 - micron/fs, 0 - lambda/t0).

---------------------------------------------------------------------
> 0		i-min	Optional subdomain (default is whole region).
> 10		i-max
> 0		j-min
> 10		j-max
> 0		k-min
> 10		k-max
